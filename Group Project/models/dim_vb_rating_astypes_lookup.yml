# Models YAML for dim_vb_rating_astypes_lookup table
version: 2
models:
  - name: dim_vb_rating_astypes_lookup
    description: "Lookup table for rating types."
    columns:
      - name: rate_astype_lookup
        description: "Type of rating."

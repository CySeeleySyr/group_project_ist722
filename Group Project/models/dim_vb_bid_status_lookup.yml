# Models YAML for dim_vb_bid_status_lookup table
version: 2
models:
  - name: dim_vb_bid_status_lookup
    description: "Lookup table for bid status values."
    columns:
      - name: bid_status
        description: "Status of the bid."
